---

- name: Copy docker-compose.yaml
  become: true
  ansible.builtin.copy:
    src: docker-compose.yaml
    dest: /srv/prod/docker-compose-ai.yaml
    mode: "0600"

- name: Copy docker-compose.yaml
  become: true
  ansible.builtin.copy:
    src: docker-compose-ollama.yaml
    dest: /srv/prod/docker-compose-ollama.yaml
    mode: "0600"
